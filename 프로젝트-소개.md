# EC2 GUI — 프로젝트 소개 (면접용)

> 신입 개발자로서 이 프로젝트를 **왜 만들었고**, **무엇을 할 수 있으며**, **어떻게 AI를 활용해 학습·구현했는지**를 면접관에게 설명하기 위한 문서입니다.

---

## 1. 한 줄 소개

**EC2 GUI**는 SSH로 원격 서버(Linux / macOS)에 접속한 뒤, 자주 쓰는 명령어를 GUI로 모아 두고 대시보드·파일·Docker·터미널을 한 앱에서 다루는 **데스크톱 원격 관리 도구**입니다.

- **형태**: Tauri 2 기반 네이티브 데스크톱 앱
- **프론트**: React 19 + TypeScript + Vite
- **백엔드**: Rust (SSH 연결, 원격 명령 실행, SFTP, PTY 터미널)

---

## 2. 만든 이유

### 2.1 업무에서 느낀 불편함

회사 업무에서 원격 서버에 SSH로 접속할 일이 많았습니다.  
CPU·메모리 확인, 디스크 용량, Docker 상태, 파일 위치 확인처럼 **자주 쓰는 작업**은 어느 정도 익숙했지만, 그 외의 명령어는 매번 검색하거나 문서를 뒤져야 했습니다.

터미널만으로도 충분히 작업은 되지만, 다음과 같은 불편이 쌓였습니다.

- 필요한 정보가 흩어져 있어 **한눈에 서버 상태를 파악하기 어렵다**
- 명령어를 외우거나 다시 찾는 비용이 반복된다
- 같은 작업을 여러 서버에서 할 때 **흐름이 끊긴다**
- “숫자만 나오는 출력”보다 **GUI로 보면 훨씬 빠르게 이해할 수 있는 정보가 많다**고 느꼈다

그래서 **SSH는 그대로 쓰되, 그 위에 사람이 보기 쉬운 GUI 레이어를 직접 만들어 보자**고 생각했습니다.

### 2.2 이미 GUI 도구가 있는데 왜 직접 만들었나?

시중에도 SSH/원격 관리용 GUI는 있습니다.  
그럼에도 직접 만든 이유는 “이미 있는 것을 대체한다”보다 **내가 느낀 문제를 기준으로 제품을 설계해 보고 싶었기 때문**입니다.

특히 중장기적으로는 **AI와 접목한 새로운 기능**을 실험하고 싶었습니다. 예를 들면 다음과 같은 방향을 염두에 두고 있습니다.

- 서버 상태를 보고 “다음에 뭘 보면 좋을지” 제안
- 자주 쓰는 점검 흐름을 자연어로 실행
- 로그·메트릭·디스크 이상 징후를 요약해 설명
- 초심자가 명령어를 몰라도 안전하게 운영 작업을 돕기

다만 **현재 버전에는 AI 기능은 아직 붙어 있지 않습니다.**  
기능부터 탄탄히 만든 뒤, “어디에 AI를 붙이면 실제로 도움이 되는지”를 먼저 설계하고 싶어서입니다.  
지금은 아래를 구상 중입니다.

- 어떤 화면/데이터에 AI가 붙어야 가치가 있는지
- 단순 챗봇이 아니라 **원격 운영 맥락을 이해하는 어시스턴트**로 만들 방법
- 민감 정보(키, 로그, 서버 설정)를 어떻게 다룰지
- 명령 실행형 AI라면 **확인·권한·되돌리기**를 어떻게 설계할지

즉, 이 프로젝트는 “완성된 AI 제품”이 아니라  
**원격 운영 GUI를 먼저 만들고, 그 위에 AI를 올릴 기반을 쌓는 단계**에 있습니다.

### 2.3 이 프로젝트를 통해 얻고 싶었던 것

- 실제 업무 페인 포인트를 제품 아이디어로 바꾸는 경험
- 프론트만이 아니라 **데스크톱 + 시스템/네트워크(SSH)** 까지의 풀스택 감각
- OS마다 다른 명령·지표를 추상화해 UI로 보여주는 설계 경험
- 이후 AI 기능을 붙일 수 있도록 **데이터와 화면을 구조화**해 두는 연습

---

## 3. 기능 설명

### 3.1 SSH 접속

- PEM 키 / 비밀번호 인증 지원
- 로그인 폼, 접속 히스토리, 자동 로그인 캐시
- 접속 유지 후 앱 전반에서 동일 세션 재사용
- 로그아웃 시 연결 정리

### 3.2 대시보드 (서버 한눈에 보기)

원격 OS를 감지해 Linux / macOS에 맞는 레이아웃으로 보여줍니다. (Windows는 Coming Soon)

| 영역 | 내용 |
|------|------|
| 시스템 리소스 | CPU, 메모리, 네트워크 등 |
| macOS 보완 | 메모리 압력, 모델/OS 버전, 업타임 등 Mac에 맞는 지표 |
| 디스크 | 파일시스템 용량 개요, 대용량 디렉터리 스캔 |
| 디스크 히스토리 | 원격에 수집 스크립트를 배포해 용량 추이 확인 |
| Docker 개요 | 컨테이너/이미지/볼륨/네트워크 요약 |
| 권한 | sudo, Docker 접근, 홈 쓰기 가능 여부 등 |
| SSH 세션 | 현재 원격에 붙어 있는 세션 목록 |

접속 직후에는 핵심 데이터를 미리 불러오는 **서버 동기화 오버레이**로, 대시보드가 바로 채워지도록 했습니다.

### 3.3 파일 탐색기

- 원격 디렉터리 탐색, 상위 이동
- 파일/폴더 생성·삭제
- 텍스트 파일 미리보기·편집
- 리스트 / 아이콘 그리드 뷰
- 파괴적 작업은 확인 다이얼로그로 안전장치 제공

### 3.4 Docker 관리

Docker가 설치된 환경에서 사이드바 메뉴가 활성화됩니다.

- 컨테이너 / 이미지 / 네트워크 / 볼륨 / 시스템 페이지
- 리스트·그리드 뷰, 상세 다이얼로그
- 컨테이너 로그 조회(필터 포함)
- start / stop / restart / remove, prune, inspect 등 운영에 필요한 액션

### 3.5 터미널

- 앱 하단 리사이즈 가능한 터미널 패널
- 멀티 탭 SSH 셸 (PTY)
- GUI로 부족한 작업은 바로 터미널로 이어갈 수 있게 설계

> GUI만으로 모든 것을 대체하지 않고, **GUI + 터미널을 함께 쓰는 워크플로**를 목표로 했습니다.

### 3.6 설정

- 테마(light / dark / system)
- 월페이퍼 프리셋·커스텀 이미지
- 앱 캐시 정리 등

---

## 4. 기술 스택과 구조

### 4.1 스택

| 구분 | 기술 |
|------|------|
| 데스크톱 | Tauri 2 |
| UI | React 19, TypeScript, Vite |
| 상태 | Zustand |
| 서버 상태/캐시 | TanStack React Query |
| 폼 | react-hook-form |
| 터미널 UI | xterm.js |
| 백엔드 | Rust, ssh2 |
| 품질 | ESLint, Prettier |

### 4.2 아키텍처 (면접에서 말하기 좋은 한 줄)

```
React UI
  → Tauri invoke / event
  → Rust commands
  → SSH 세션 매니저 + OS별 remote 서비스
  → 원격 호스트 (exec / SFTP / PTY)
```

프론트는 화면과 사용자 흐름에 집중하고,  
실제 SSH·파일·Docker·시스템 조회는 Rust 쪽에서 처리합니다.  
Linux와 macOS는 명령어·지표가 달라서, OS 감지 후 각각 다른 remote 서비스로 분기합니다.

### 4.3 구현에서 신경 쓴 점

- **세션 단위 React Query 키**: 호스트/포트/유저별로 캐시를 분리
- **무거운 작업 후순위**: 예) `du` 같은 비용 큰 조회는 핵심 메트릭 이후
- **OS별 레이아웃·기능 게이팅**: 지원하지 않는 OS/기능은 Coming Soon으로 분리
- **Docker 개요 API**: 대시보드에서 목록 API를 여러 번 치지 않도록 요약 조회
- **파일 미리보기 안전장치**: 바이너리 감지, 미리보기 크기 제한
- **권한 스냅샷**: “이 계정으로 어디까지 할 수 있는지”를 먼저 보여 운영 실수를 줄임

---

## 5. AI 활용 능력 (면접에서 강조할 포인트)

이 프로젝트는 “AI가 코드를 대신 짜 준 결과물”로 말하기보다,  
**문제를 정의하고 → 요구사항을 쪼개고 → AI와 협업해 구현·검증한 결과물**로 설명하는 것이 맞습니다.

### 5.1 내가 한 일 (사람 역할)

- 업무 중 느낀 불편을 **제품 문제 정의**로 바꿈
- “SSH GUI”라는 큰 목표를 대시보드 / 파일 / Docker / 터미널로 **기능 단위 분해**
- Linux와 macOS 차이를 문서화하고, 우선순위(로드맵)를 정함
- UI 흐름, 안전장치(확인 다이얼로그, 권한 표시), 터미널 병행 사용 같은 **제품 결정**
- AI가 제안한 코드를 그대로 쓰지 않고, 동작·보안·UX 기준으로 **검토·수정**

### 5.2 AI를 어떻게 활용했는지

- 낯선 영역(Tauri, Rust SSH, PTY, OS별 시스템 명령)의 **학습 가속**
- 반복 보일러플레이트·타입 정의·화면 골격의 **생산성 향상**
- 버그 원인 추적 시 로그·증상 기반 **가설 수립과 수정안 비교**
- 리팩터링 시 구조 제안 받기 → 내가 도메인에 맞게 취사선택

### 5.3 면접에서 이렇게 말할 수 있습니다

> “바이브코딩으로 만들었다”가 아니라,  
> **AI를 시니어 페어 프로그래머처럼 쓰되, 문제 정의·우선순위·검증·책임은 제가 가져갔습니다.**  
> 특히 원격 서버를 다루는 앱이라서, 명령 실행·권한·파괴적 액션은 AI 제안이라도 반드시 직접 확인했습니다.  
> 앞으로는 이 GUI 위에 AI 기능을 붙이려 하고, 지금은 ‘어디에 붙이면 실사용 가치가 있는지’를 설계하는 단계입니다.

---

## 6. 현재 상태와 앞으로의 계획

### 현재

- Linux / macOS 원격 관리의 핵심 플로우(접속 → 대시보드 → 파일/Docker/터미널) 구현
- OS별 지표·레이아웃 분기
- AI 기능은 미구현 (기반 GUI 우선)

### 앞으로

1. **AI 기능 설계 구체화**  
   - 예: 대시보드 이상 징후 설명, 점검 체크리스트 생성, 안전한 명령 제안
2. macOS 원격 기능 확장  
   - FileVault / SIP, 배터리·절전, LaunchAgents, Homebrew services 등
3. Windows 원격 지원
4. 권한·감사·실행 확인 UX 강화 (AI 실행형 기능을 염두에 둔 안전장치)

---

## 7. 면접 Q&A 대비 (짧게)

**Q. 왜 터미널 대신 GUI인가?**  
A. 터미널을 없애려는 게 아니라, 자주 보는 정보를 먼저 GUI로 모아 두고 필요할 때만 터미널로 내려가게 하려고 만들었습니다. 앱 안에 터미널도 포함되어 있습니다.

**Q. 기존 도구와 차별점은?**  
A. 당장은 “AI 연동을 전제로 한 원격 운영 GUI”를 직접 설계·확장할 수 있다는 점이 목적입니다. 지금은 기반 기능을 쌓는 중이고, AI는 붙이기 전 설계 단계입니다.

**Q. 가장 어려웠던 점은?**  
A. OS마다 명령과 의미가 달라서(예: Linux `/proc` vs macOS `memory_pressure`) 같은 “서버 상태”를 일관된 UI로 보여주는 부분이었습니다.

**Q. AI로만 만든 거 아닌가?**  
A. AI는 학습과 구현 속도를 높이는 도구로 썼고, 문제 정의·기능 분해·우선순위·검증·보안/UX 판단은 제가 했습니다. 특히 SSH·삭제·권한 관련은 제안 코드를 그대로 신뢰하지 않았습니다.

---

## 8. 기술 한눈에 보기

- **앱 이름**: EC2 GUI
- **유형**: SSH 원격 서버 관리 데스크톱 앱
- **핵심 화면**: Login → Dashboard / Files / Docker / Settings + Terminal
- **핵심 가치**: “명령어를 찾는 시간”을 줄이고, 서버 상태를 빠르게 이해하게 돕는 것
- **다음 목표**: 원격 운영 맥락을 이해하는 AI 어시스턴트 접목
